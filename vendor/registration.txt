🏪 Vendor Auth Components - Complete Implementation

📝 1. Registration Wizard (Main Component)

```blade
{{-- components/vendor/auth/registration-wizard.blade.php --}}
@props([
    'currentStep' => 1,
    'totalSteps' => 4,
    'formData' => [],
    'errors' => []
])

<div class="vendor-registration-wizard bg-white rounded-xl shadow-lg overflow-hidden">
    <!-- Progress Header -->
    <div class="bg-gray-50 px-6 py-4 border-b">
        <div class="flex items-center justify-between">
            <div>
                <h1 class="text-2xl font-bold text-gray-900">เปิดร้านค้าบน Blife</h1>
                <p class="text-gray-600">กรอกข้อมูลให้ครบถ้วนเพื่อเริ่มขายสินค้า</p>
            </div>
            <div class="text-right">
                <div class="text-sm text-gray-500">ขั้นตอนที่</div>
                <div class="text-xl font-bold text-blue-600">{{ $currentStep }}/{{ $totalSteps }}</div>
            </div>
        </div>
    </div>

    <!-- Progress Bar -->
    <div class="px-6 pt-4">
        <div class="w-full bg-gray-200 rounded-full h-2">
            <div class="bg-blue-600 h-2 rounded-full transition-all duration-300" 
                 style="width: {{ ($currentStep / $totalSteps) * 100 }}%"></div>
        </div>
    </div>

    <!-- Steps Navigation -->
    <div class="px-6 pb-4">
        <div class="flex justify-between text-sm">
            <x-vendor.auth.registration-step 
                number="1" 
                label="ข้อมูลส่วนตัว"
                :active="$currentStep === 1"
                :completed="$currentStep > 1"
            />
            <x-vendor.auth.registration-step 
                number="2" 
                label="ตั้งค่าร้านค้า"
                :active="$currentStep === 2"
                :completed="$currentStep > 2"
            />
            <x-vendor.auth.registration-step 
                number="3" 
                label="ข้อมูลธุรกิจ"
                :active="$currentStep === 3"
                :completed="$currentStep > 3"
            />
            <x-vendor.auth.registration-step 
                number="4" 
                label="ยืนยันการสมัคร"
                :active="$currentStep === 4"
                :completed="$currentStep > 4"
            />
        </div>
    </div>

    <!-- Step Content -->
    <div class="p-6">
        @if($currentStep === 1)
            <x-vendor.auth.personal-info-form 
                :formData="$formData"
                :errors="$errors"
            />
        @elseif($currentStep === 2)
            <x-vendor.auth.shop-setup-form 
                :formData="$formData"
                :errors="$errors"
            />
        @elseif($currentStep === 3)
            <x-vendor.auth.business-info-form 
                :formData="$formData"
                :errors="$errors"
            />
        @elseif($currentStep === 4)
            <x-vendor.auth.confirmation-step 
                :formData="$formData"
                :errors="$errors"
            />
        @endif
    </div>

    <!-- Global Error Display -->
    @if($errors->any())
        <div class="px-6 pb-4">
            <x-global.error-message 
                type="validation"
                :errors="$errors"
                thai-messages="true"
            />
        </div>
    @endif
</div>
```

🔢 2. Registration Step Indicator

```blade
{{-- components/vendor/auth/registration-step.blade.php --}}
@props([
    'number' => 1,
    'label' => 'Step',
    'active' => false,
    'completed' => false
])

@php
    $baseClasses = "flex flex-col items-center flex-1 transition-all duration-300";
    $activeClasses = "text-blue-600 font-semibold";
    $completedClasses = "text-green-600 font-semibold";
    $inactiveClasses = "text-gray-400";
    
    $textClass = $active ? $activeClasses : ($completed ? $completedClasses : $inactiveClasses);
@endphp

<div {{ $attributes->merge(['class' => $baseClasses]) }}>
    <div class="flex items-center justify-center w-8 h-8 rounded-full mb-2 
                {{ $active ? 'bg-blue-600 text-white' : 
                   ($completed ? 'bg-green-500 text-white' : 'bg-gray-200 text-gray-500') }}">
        @if($completed)
            <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd" />
            </svg>
        @else
            {{ $number }}
        @endif
    </div>
    <span class="text-xs {{ $textClass }} text-center">{{ $label }}</span>
    
    <!-- Connector Line -->
    @if($number < 4)
        <div class="absolute top-4 left-1/2 w-full h-0.5 -z-10 
                    {{ $completed ? 'bg-green-500' : 'bg-gray-200' }}"></div>
    @endif
</div>
```

👤 3. Personal Information Form

```blade
{{-- components/vendor/auth/personal-info-form.blade.php --}}
@props([
    'formData' => [],
    'errors' => []
])

<div class="personal-info-form">
    <div class="text-center mb-8">
        <div class="w-16 h-16 bg-blue-100 rounded-full flex items-center justify-center mx-auto mb-4">
            <svg class="w-8 h-8 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z" />
            </svg>
        </div>
        <h2 class="text-2xl font-bold text-gray-900 mb-2">ข้อมูลส่วนตัว</h2>
        <p class="text-gray-600">กรอกข้อมูลส่วนตัวเพื่อใช้ในการติดต่อและยืนยันตัวตน</p>
    </div>

    <form wire:submit="saveStep1">
        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
            <!-- First Name -->
            <div>
                <x-global.form-input
                    label="ชื่อจริง"
                    name="first_name"
                    wire:model="formData.first_name"
                    :error="$errors->first('first_name')"
                    required
                    thai-label
                    placeholder="เช่น สมชาย"
                    icon="👤"
                />
            </div>

            <!-- Last Name -->
            <div>
                <x-global.form-input
                    label="นามสกุล"
                    name="last_name"
                    wire:model="formData.last_name"
                    :error="$errors->first('last_name')"
                    required
                    thai-label
                    placeholder="เช่น ใจดี"
                    icon="👤"
                />
            </div>

            <!-- Email -->
            <div class="md:col-span-2">
                <x-global.form-input
                    label="อีเมล"
                    type="email"
                    name="email"
                    wire:model="formData.email"
                    :error="$errors->first('email')"
                    required
                    thai-label
                    placeholder="เช่น somchai@example.com"
                    icon="📧"
                />
            </div>

            <!-- Phone -->
            <div class="md:col-span-2">
                <x-global.form-input
                    label="เบอร์โทรศัพท์"
                    type="tel"
                    name="phone"
                    wire:model="formData.phone"
                    :error="$errors->first('phone')"
                    required
                    thai-label
                    placeholder="เช่น 0812345678"
                    icon="📱"
                />
            </div>

            <!-- Password -->
            <div>
                <x-global.form-input
                    label="รหัสผ่าน"
                    type="password"
                    name="password"
                    wire:model="formData.password"
                    :error="$errors->first('password')"
                    required
                    thai-label
                    placeholder="อย่างน้อย 8 ตัวอักษร"
                    icon="🔒"
                />
            </div>

            <!-- Confirm Password -->
            <div>
                <x-global.form-input
                    label="ยืนยันรหัสผ่าน"
                    type="password"
                    name="password_confirmation"
                    wire:model="formData.password_confirmation"
                    :error="$errors->first('password_confirmation')"
                    required
                    thai-label
                    placeholder="กรอกรหัสผ่านอีกครั้ง"
                    icon="🔒"
                />
            </div>
        </div>

        <!-- Terms Agreement -->
        <div class="mt-6 p-4 bg-blue-50 rounded-lg">
            <label class="flex items-start space-x-3">
                <input 
                    type="checkbox" 
                    wire:model="formData.agree_terms"
                    class="mt-1 rounded border-gray-300 text-blue-600 focus:ring-blue-500"
                >
                <span class="text-sm text-gray-700">
                    ฉันยอมรับ 
                    <a href="{{ route('terms') }}" target="_blank" class="text-blue-600 hover:text-blue-800 font-medium">
                        ข้อกำหนดและเงื่อนไข
                    </a> 
                    และ 
                    <a href="{{ route('privacy') }}" target="_blank" class="text-blue-600 hover:text-blue-800 font-medium">
                        นโยบายความเป็นส่วนตัว
                    </a> 
                    ของ Blife Platform
                </span>
            </label>
            @error('formData.agree_terms')
                <p class="mt-1 text-sm text-red-600">{{ $message }}</p>
            @enderror
        </div>

        <!-- Navigation -->
        <div class="flex justify-end mt-8 pt-6 border-t">
            <button 
                type="submit"
                class="btn btn-primary px-8 py-3"
                wire:loading.attr="disabled"
            >
                <span wire:loading.remove>ถัดไป → ตั้งค่าร้านค้า</span>
                <span wire:loading>กำลังตรวจสอบ...</span>
            </button>
        </div>
    </form>
</div>
```

🏪 4. Shop Setup Form

```blade
{{-- components/vendor/auth/shop-setup-form.blade.php --}}
@props([
    'formData' => [],
    'errors' => [],
    'categories' => []
])

<div class="shop-setup-form">
    <div class="text-center mb-8">
        <div class="w-16 h-16 bg-green-100 rounded-full flex items-center justify-center mx-auto mb-4">
            <svg class="w-8 h-8 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4" />
            </svg>
        </div>
        <h2 class="text-2xl font-bold text-gray-900 mb-2">ตั้งค่าร้านค้า</h2>
        <p class="text-gray-600">กำหนดข้อมูลร้านค้าที่จะแสดงต่อลูกค้า</p>
    </div>

    <form wire:submit="saveStep2">
        <div class="space-y-6">
            <!-- Shop Name -->
            <div>
                <x-global.form-input
                    label="ชื่อร้านค้า"
                    name="shop_name"
                    wire:model="formData.shop_name"
                    :error="$errors->first('shop_name')"
                    required
                    thai-label
                    placeholder="เช่น ร้านค้าดีๆ สินค้าคุณภาพ"
                    icon="🏪"
                    help-text="ชื่อร้านค้าที่จะแสดงต่อลูกค้า ต้องไม่ซ้ำกับร้านอื่น"
                />
            </div>

            <!-- Shop Description -->
            <div>
                <x-global.form-textarea
                    label="รายละเอียดร้านค้า"
                    name="shop_description"
                    wire:model="formData.shop_description"
                    :error="$errors->first('shop_description')"
                    required
                    rows="4"
                    thai-label
                    placeholder="อธิบายเกี่ยวกับร้านค้าของคุณ สินค้าที่จำหน่าย จุดเด่นของร้าน"
                    help-text="อธิบายให้ลูกค้าทราบเกี่ยวกับร้านค้าของคุณอย่างน้อย 50 ตัวอักษร"
                />
            </div>

            <!-- Shop Category -->
            <div>
                <x-global.form-select
                    label="หมวดหมู่ร้านค้า"
                    name="shop_category"
                    wire:model="formData.shop_category"
                    :error="$errors->first('shop_category')"
                    :options="$categories"
                    required
                    thai-label
                    placeholder="เลือกหมวดหมู่ที่เหมาะสมกับร้านค้า"
                    help-text="เลือกหมวดหมู่ที่ตรงกับสินค้าที่คุณจะขาย"
                />
            </div>

            <!-- Shop Logo Upload -->
            <div>
                <label class="block text-sm font-medium text-gray-700 mb-2">
                    🖼️ ล็อโก้ร้านค้า
                </label>
                <x-global.file-upload
                    wire:model="formData.shop_logo"
                    :error="$errors->first('shop_logo')"
                    accept="image/*"
                    max-size="2MB"
                    help-text="รูปภาพสี่เหลี่ยมจตุรัส ขนาดแนะนำ 400x400 px รองรับ PNG, JPG ขนาดไม่เกิน 2MB"
                />
                
                @if($formData['shop_logo'] ?? false)
                    <div class="mt-2">
                        <p class="text-sm text-green-600">✓ อัพโหลดสำเร็จ</p>
                        <img src="{{ $formData['shop_logo']->temporaryUrl() }}" 
                             alt="Shop logo preview" 
                             class="mt-2 w-20 h-20 rounded-lg object-cover">
                    </div>
                @endif
            </div>

            <!-- Shop Banner Upload -->
            <div>
                <label class="block text-sm font-medium text-gray-700 mb-2">
                    🎨 แบนเนอร์ร้านค้า
                </label>
                <x-global.file-upload
                    wire:model="formData.shop_banner"
                    :error="$errors->first('shop_banner')"
                    accept="image/*"
                    max-size="5MB"
                    help-text="รูปภาพแนวนอน ขนาดแนะนำ 1200x300 px รองรับ PNG, JPG ขนาดไม่เกิน 5MB"
                />
                
                @if($formData['shop_banner'] ?? false)
                    <div class="mt-2">
                        <p class="text-sm text-green-600">✓ อัพโหลดสำเร็จ</p>
                        <img src="{{ $formData['shop_banner']->temporaryUrl() }}" 
                             alt="Shop banner preview" 
                             class="mt-2 w-full h-32 rounded-lg object-cover">
                    </div>
                @endif
            </div>
        </div>

        <!-- Navigation -->
        <div class="flex justify-between mt-8 pt-6 border-t">
            <button 
                type="button"
                wire:click="previousStep"
                class="btn btn-outline px-6 py-3"
            >
                ← ย้อนกลับ
            </button>
            
            <button 
                type="submit"
                class="btn btn-primary px-8 py-3"
                wire:loading.attr="disabled"
            >
                <span wire:loading.remove>ถัดไป → ข้อมูลธุรกิจ</span>
                <span wire:loading>กำลังบันทึก...</span>
            </button>
        </div>
    </form>
</div>
```

💼 5. Business Information Form

```blade
{{-- components/vendor/auth/business-info-form.blade.php --}}
@props([
    'formData' => [],
    'errors' => [],
    'provinces' => [],
    'districts' => [],
    'subdistricts' => []
])

<div class="business-info-form">
    <div class="text-center mb-8">
        <div class="w-16 h-16 bg-purple-100 rounded-full flex items-center justify-center mx-auto mb-4">
            <svg class="w-8 h-8 text-purple-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4" />
            </svg>
        </div>
        <h2 class="text-2xl font-bold text-gray-900 mb-2">ข้อมูลธุรกิจ</h2>
        <p class="text-gray-600">กรอกข้อมูลที่อยู่ธุรกิจและการชำระเงินสำหรับรับเงิน</p>
    </div>

    <form wire:submit="saveStep3">
        <div class="space-y-8">
            <!-- Business Address Section -->
            <div class="bg-gray-50 rounded-lg p-6">
                <h3 class="text-lg font-semibold text-gray-900 mb-4 flex items-center">
                    <svg class="w-5 h-5 text-gray-600 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z" />
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z" />
                    </svg>
                    ที่อยู่ธุรกิจ
                </h3>
                
                <x-global.thai-address-form
                    :provinces="$provinces"
                    :districts="$districts"
                    :subdistricts="$subdistricts"
                    :selectedProvince="$formData['business_province'] ?? null"
                    :selectedDistrict="$formData['business_district'] ?? null"
                    :selectedSubdistrict="$formData['business_subdistrict'] ?? null"
                    :zipcode="$formData['business_zipcode'] ?? null"
                    wire:model="formData"
                    :errors="$errors"
                />

                <!-- Address Details -->
                <div class="mt-4">
                    <x-global.form-textarea
                        label="ที่อยู่บรรทัดที่ 1"
                        name="business_address_line1"
                        wire:model="formData.business_address_line1"
                        :error="$errors->first('business_address_line1')"
                        required
                        rows="2"
                        thai-label
                        placeholder="เลขที่บ้าน หมู่บ้าน อาคาร ถนน"
                    />
                </div>
            </div>

            <!-- Bank Information Section -->
            <div class="bg-gray-50 rounded-lg p-6">
                <h3 class="text-lg font-semibold text-gray-900 mb-4 flex items-center">
                    <svg class="w-5 h-5 text-gray-600 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 10h18M7 15h1m4 0h1m-7 4h12a3 3 0 003-3V8a3 3 0 00-3-3H6a3 3 0 00-3 3v8a3 3 0 003 3z" />
                    </svg>
                    ข้อมูลบัญชีธนาคาร
                </h3>

                <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
                    <!-- Bank Name -->
                    <div>
                        <x-global.form-select
                            label="ธนาคาร"
                            name="bank_name"
                            wire:model="formData.bank_name"
                            :error="$errors->first('bank_name')"
                            :options="[
                                'kbank' => 'ธนาคารกสิกรไทย',
                                'bbl' => 'ธนาคารกรุงเทพ',
                                'scb' => 'ธนาคารไทยพาณิชย์',
                                'ktb' => 'ธนาคารกรุงไทย',
                                'bay' => 'ธนาคารกรุงศรีอยุธยา',
                                'tmb' => 'ธนาคารทหารไทย',
                                'cimb' => 'ธนาคารซีไอเอ็มบี',
                                'uob' => 'ธนาคารยูโอบี',
                                'tisco' => 'ธนาคารทิสโก้',
                                'kk' => 'ธนาคารเกียรตินาคิน',
                            ]"
                            required
                            thai-label
                            placeholder="เลือกธนาคาร"
                        />
                    </div>

                    <!-- Account Number -->
                    <div>
                        <x-global.form-input
                            label="เลขที่บัญชี"
                            name="bank_account_number"
                            wire:model="formData.bank_account_number"
                            :error="$errors->first('bank_account_number')"
                            required
                            thai-label
                            placeholder="เช่น 1234567890"
                            icon="🔢"
                        />
                    </div>

                    <!-- Account Name -->
                    <div>
                        <x-global.form-input
                            label="ชื่อบัญชี"
                            name="bank_account_name"
                            wire:model="formData.bank_account_name"
                            :error="$errors->first('bank_account_name')"
                            required
                            thai-label
                            placeholder="ชื่อ-นามสกุล เต็มตามบัญชี"
                            icon="👤"
                        />
                    </div>
                </div>

                <!-- Account Verification -->
                <div class="mt-4 p-4 bg-blue-50 rounded-lg">
                    <div class="flex items-center">
                        <svg class="w-5 h-5 text-blue-600 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
                        </svg>
                        <span class="text-sm text-blue-800">
                            ชื่อบัญชีต้องตรงกับชื่อจริงที่ลงทะเบียน เพื่อป้องกันการฉ้อโกง
                        </span>
                    </div>
                </div>
            </div>

            <!-- Tax Information (Optional) -->
            <div class="bg-gray-50 rounded-lg p-6">
                <h3 class="text-lg font-semibold text-gray-900 mb-4">ข้อมูลภาษี (ถ้ามี)</h3>
                
                <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                    <x-global.form-input
                        label="เลขประจำตัวผู้เสียภาษี"
                        name="tax_id"
                        wire:model="formData.tax_id"
                        :error="$errors->first('tax_id')"
                        thai-label
                        placeholder="เช่น 0123456789012"
                        help-text="กรอกเฉพาะหากมีเลขประจำตัวผู้เสียภาษี"
                    />

                    <x-global.form-input
                        label="ชื่อนิติบุคคล (ถ้ามี)"
                        name="company_name"
                        wire:model="formData.company_name"
                        :error="$errors->first('company_name')"
                        thai-label
                        placeholder="ชื่อบริษัทตามทะเบียน"
                    />
                </div>
            </div>
        </div>

        <!-- Navigation -->
        <div class="flex justify-between mt-8 pt-6 border-t">
            <button 
                type="button"
                wire:click="previousStep"
                class="btn btn-outline px-6 py-3"
            >
                ← ย้อนกลับ
            </button>
            
            <button 
                type="submit"
                class="btn btn-primary px-8 py-3"
                wire:loading.attr="disabled"
            >
                <span wire:loading.remove>ถัดไป → ยืนยันการสมัคร</span>
                <span wire:loading>กำลังบันทึก...</span>
            </button>
        </div>
    </form>
</div>
```

✅ 6. Confirmation Step

```blade
{{-- components/vendor/auth/confirmation-step.blade.php --}}
@props([
    'formData' => [],
    'errors' => []
])

<div class="confirmation-step">
    <div class="text-center mb-8">
        <div class="w-16 h-16 bg-green-100 rounded-full flex items-center justify-center mx-auto mb-4">
            <svg class="w-8 h-8 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
            </svg>
        </div>
        <h2 class="text-2xl font-bold text-gray-900 mb-2">ยืนยันข้อมูลการสมัคร</h2>
        <p class="text-gray-600">ตรวจสอบข้อมูลให้ครบถ้วนก่อนส่งคำขอกำหนดร้านค้า</p>
    </div>

    <!-- Summary Cards -->
    <div class="space-y-6 mb-8">
        <!-- Personal Information -->
        <div class="bg-white border border-gray-200 rounded-lg p-6">
            <h3 class="text-lg font-semibold text-gray-900 mb-4 flex items-center">
                <svg class="w-5 h-5 text-blue-600 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z" />
                </svg>
                ข้อมูลส่วนตัว
            </h3>
            <dl class="grid grid-cols-1 md:grid-cols-2 gap-4 text-sm">
                <div class="flex">
                    <dt class="w-1/3 text-gray-600">ชื่อ-สกุล:</dt>
                    <dd class="font-medium">{{ $formData['first_name'] ?? '' }} {{ $formData['last_name'] ?? '' }}</dd>
                </div>
                <div class="flex">
                    <dt class="w-1/3 text-gray-600">อีเมล:</dt>
                    <dd class="font-medium">{{ $formData['email'] ?? '' }}</dd>
                </div>
                <div class="flex">
                    <dt class="w-1/3 text-gray-600">โทรศัพท์:</dt>
                    <dd class="font-medium">{{ $formData['phone'] ?? '' }}</dd>
                </div>
            </dl>
        </div>

        <!-- Shop Information -->
        <div class="bg-white border border-gray-200 rounded-lg p-6">
            <h3 class="text-lg font-semibold text-gray-900 mb-4 flex items-center">
                <svg class="w-5 h-5 text-green-600 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4" />
                </svg>
                ข้อมูลร้านค้า
            </h3>
            <dl class="grid grid-cols-1 md:grid-cols-2 gap-4 text-sm">
                <div class="flex">
                    <dt class="w-1/3 text-gray-600">ชื่อร้าน:</dt>
                    <dd class="font-medium">{{ $formData['shop_name'] ?? '' }}</dd>
                </div>
                <div class="flex">
                    <dt class="w-1/3 text-gray-600">หมวดหมู่:</dt>
                    <dd class="font-medium">{{ $formData['shop_category_name'] ?? '' }}</dd>
                </div>
                <div class="md:col-span-2">
                    <dt class="text-gray-600 mb-1">รายละเอียดร้าน:</dt>
                    <dd class="font-medium">{{ $formData['shop_description'] ?? '' }}</dd>
                </div>
            </dl>
        </div>

        <!-- Business Information -->
        <div class="bg-white border border-gray-200 rounded-lg p-6">
            <h3 class="text-lg font-semibold text-gray-900 mb-4 flex items-center">
                <svg class="w-5 h-5 text-purple-600 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4" />
                </svg>
                ข้อมูลธุรกิจ
            </h3>
            <dl class="grid grid-cols-1 md:grid-cols-2 gap-4 text-sm">
                <div class="flex">
                    <dt class="w-1/3 text-gray-600">ธนาคาร:</dt>
                    <dd class="font-medium">{{ $formData['bank_name'] ?? '' }}</dd>
                </div>
                <div class="flex">
                    <dt class="w-1/3 text-gray-600">เลขที่บัญชี:</dt>
                    <dd class="font-medium">{{ $formData['bank_account_number'] ?? '' }}</dd>
                </div>
                <div class="flex">
                    <dt class="w-1/3 text-gray-600">ชื่อบัญชี:</dt>
                    <dd class="font-medium">{{ $formData['bank_account_name'] ?? '' }}</dd>
                </div>
                <div class="md:col-span-2">
                    <dt class="text-gray-600 mb-1">ที่อยู่:</dt>
                    <dd class="font-medium">{{ $formData['business_address_line1'] ?? '' }}, {{ $formData['business_province_name'] ?? '' }}</dd>
                </div>
            </dl>
        </div>
    </div>

    <!-- Final Agreement -->
    <div class="bg-yellow-50 border border-yellow-200 rounded-lg p-6 mb-6">
        <h4 class="font-semibold text-yellow-800 mb-3 flex items-center">
            <svg class="w-5 h-5 mr-2" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M8.257 3.099c.765-1.36 2.722-1.36 3.486 0l5.58 9.92c.75 1.334-.213 2.98-1.742 2.98H4.42c-1.53 0-2.493-1.646-1.743-2.98l5.58-9.92zM11 13a1 1 0 11-2 0 1 1 0 012 0zm-1-8a1 1 0 00-1 1v3a1 1 0 002 0V6a1 1 0 00-1-1z" clip-rule="evenodd" />
            </svg>
            ข้อตกลงสำคัญ
        </h4>
        <ul class="text-sm text-yellow-700 space-y-2">
            <li>• ข้อมูลทั้งหมดต้องถูกต้องและเป็นความจริง</li>
            <li>• ทางระบบจะทำการตรวจสอบข้อมูลภายใน 1-3 วันทำการ</li>
            <li>• การอนุมัติร้านค้าขึ้นอยู่กับดุลยพินิจของทีมงาน</li>
            <li>• ต้องปฏิบัติตามข้อกำหนดและนโยบายของ Blife Platform</li>
        </ul>
    </div>

    <!-- Final Confirmation -->
    <form wire:submit="submitRegistration">
        <div class="bg-blue-50 rounded-lg p-6 mb-6">
            <label class="flex items-start space-x-3">
                <input 
                    type="checkbox" 
                    wire:model="formData.final_agreement"
                    class="mt-1 rounded border-gray-300 text-blue-600 focus:ring-blue-500"
                    required
                >
                <span class="text-sm text-gray-700">
                    ฉันยืนยันว่าข้อมูลทั้งหมดที่กรอกถูกต้องเป็นความจริง และตกลงที่จะปฏิบัติตาม
                    <a href="{{ route('vendor.terms') }}" target="_blank" class="text-blue-600 hover:text-blue-800 font-medium">
                        ข้อตกลงสำหรับผู้ขาย
                    </a> 
                    ของ Blife Platform อย่างเคร่งครัด
                </span>
            </label>
            @error('formData.final_agreement')
                <p class="mt-2 text-sm text-red-600">{{ $message }}</p>
            @enderror
        </div>

        <!-- Navigation -->
        <div class="flex justify-between pt-6 border-t">
            <button 
                type="button"
                wire:click="previousStep"
                class="btn btn-outline px-6 py-3"
            >
                ← ย้อนกลับ
            </button>
            
            <button 
                type="submit"
                class="btn btn-success px-8 py-3"
                wire:loading.attr="disabled"
                {{ !($formData['final_agreement'] ?? false) ? 'disabled' : '' }}
            >
                <span wire:loading.remove>
                    ✅ ส่งคำขอกำหนดร้านค้า
                </span>
                <span wire:loading>
                    ⏳ กำลังส่งคำขอ...
                </span>
            </button>
        </div>
    </form>
</div>
```

🎨 Supporting CSS Styles

```scss
{{-- resources/css/components/vendor/auth.scss --}}
.vendor-registration-wizard {
    .btn {
        @apply inline-flex items-center px-4 py-2 border text-sm font-medium rounded-md transition-colors duration-200;
        
        &-primary {
            @apply border-transparent text-white bg-blue-600 hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500;
        }
        
        &-outline {
            @apply border-gray-300 text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500;
        }
        
        &-success {
            @apply border-transparent text-white bg-green-600 hover:bg-green-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-green-500;
        }
        
        &:disabled {
            @apply opacity-50 cursor-not-allowed;
        }
    }
    
    .registration-step {
        position: relative;
        
        &:not(:last-child)::after {
            content: '';
            position: absolute;
            top: 1rem;
            left: 50%;
            width: 100%;
            height: 2px;
            background: #e5e7eb;
            z-index: -1;
        }
    }
    
    .form-input, .form-select, .form-textarea {
        @apply block w-full border-gray-300 rounded-md shadow-sm focus:border-blue-500 focus:ring-blue-500;
    }
    
    .file-upload {
        @apply border-2 border-dashed border-gray-300 rounded-lg p-6 text-center hover:border-gray-400 transition-colors;
    }
}
```

🔧 Livewire Component Integration

```php
<?php
// app/Livewire/VendorRegistration.php

namespace App\Livewire;

use Livewire\Component;
use Livewire\WithFileUploads;
use App\Services\VendorRegistrationService;

class VendorRegistration extends Component
{
    use WithFileUploads;

    public $currentStep = 1;
    public $formData = [];
    
    // Step validation rules
    protected $rules = [
        // Step 1
        'formData.first_name' => 'required|string|max:100',
        'formData.last_name' => 'required|string|max:100',
        'formData.email' => 'required|email|unique:users,email',
        'formData.phone' => 'required|string|max:20',
        'formData.password' => 'required|min:8|confirmed',
        'formData.agree_terms' => 'accepted',
        
        // Step 2
        'formData.shop_name' => 'required|string|max:255|unique:vendors,shop_name',
        'formData.shop_description' => 'required|string|min:50',
        'formData.shop_category' => 'required|exists:categories,id',
        'formData.shop_logo' => 'nullable|image|max:2048',
        'formData.shop_banner' => 'nullable|image|max:5120',
        
        // Step 3
        'formData.bank_name' => 'required|string',
        'formData.bank_account_number' => 'required|string',
        'formData.bank_account_name' => 'required|string',
        'formData.business_province' => 'required',
        'formData.business_district' => 'required',
        'formData.business_subdistrict' => 'required',
        'formData.business_address_line1' => 'required',
        
        // Step 4
        'formData.final_agreement' => 'accepted',
    ];

    public function saveStep1()
    {
        $this->validateOnly([
            'formData.first_name',
            'formData.last_name', 
            'formData.email',
            'formData.phone',
            'formData.password',
            'formData.agree_terms'
        ]);

        $this->currentStep = 2;
    }

    public function saveStep2()
    {
        $this->validateOnly([
            'formData.shop_name',
            'formData.shop_description',
            'formData.shop_category',
            'formData.shop_logo',
            'formData.shop_banner'
        ]);

        $this->currentStep = 3;
    }

    public function saveStep3()
    {
        $this->validateOnly([
            'formData.bank_name',
            'formData.bank_account_number',
            'formData.bank_account_name',
            'formData.business_province',
            'formData.business_district', 
            'formData.business_subdistrict',
            'formData.business_address_line1'
        ]);

        $this->currentStep = 4;
    }

    public function submitRegistration()
    {
        $this->validate();

        try {
            $service = app(VendorRegistrationService::class);
            $vendor = $service->register($this->formData);
            
            // Redirect to success page
            return redirect()->route('vendor.registration.success');
            
        } catch (\Exception $e) {
            $this->addError('registration', 'การสมัครไม่สำเร็จ: ' . $e->getMessage());
        }
    }

    public function previousStep()
    {
        if ($this->currentStep > 1) {
            $this->currentStep--;
        }
    }

    public function render()
    {
        return view('livewire.vendor-registration')
            ->layout('components.layouts.auth');
    }
}
```

ระบบ Vendor Registration นี้รองรับการสมัครผู้ขายแบบหลายขั้นตอน พร้อมการตรวจสอบข้อมูลและการอัพโหลดไฟล์อย่างสมบูรณ์